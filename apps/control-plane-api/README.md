# Orus Control Plane API

API produit gerant projets, sources et executions. Les metadonnees sont dans
SQLite, les fichiers et rapports dans un stockage local configurable. PostgreSQL
reste reserve au graphe ontologique.
